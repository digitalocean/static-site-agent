# Copy to .env and fill in your values so `docker compose up` gets these in the container.
# Optional: use one of the API keys for the LLM.
# OPENAI_API_KEY=sk-...
DO_GRADIENT_API_KEY=

# Required for deploy_to_spaces (create bucket and upload files).
SPACES_ACCESS_KEY_ID=
SPACES_SECRET_ACCESS_KEY=
